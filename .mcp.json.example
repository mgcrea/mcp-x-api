{
  "mcpServers": {
    "x": {
      "command": "npx",
      "args": ["-y", "@mgcrea/mcp-x"],
      "env": {
        "X_BEARER_TOKEN": "your-app-only-bearer-token",
        "X_DEFAULT_MAX_RESULTS": "10",
        "X_MONTHLY_BUDGET_USD": "25"
      }
    }
  }
}
